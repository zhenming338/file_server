spring:
  application:
    name: file-server
  servlet:
    multipart:
      max-file-size: 1GB
      max-request-size: 5GB
  profiles:
    include:
      - database
      - file-dev
server:
  port: 9998

