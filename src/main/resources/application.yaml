spring:
  application:
    name: file-server
  servlet:
    multipart:
      max-file-size: 1GB
      max-request-size: 5GB
server:
  port: 9999
file:
  path: /data/fileServer/
  host: 127.0.0.1
  port: ${server.port}
