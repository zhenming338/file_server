spring:
  profiles:
    include:
      - remote
  application:
    name: file-server
  servlet:
    multipart:
      max-file-size: 5GB
      max-request-size: 10GB
server:
  port: 9999
logging:
  level:
    org.river.file_server.controller: debug
